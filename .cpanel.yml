---
# Publicação na Mindnet (cPanel → Git Version Control → "Deploy HEAD Commit").
# Copia só o que é site para public_html; LEIA-ME e CLAUDE.md ficam de fora.
# Observação: arquivo apagado no repositório NÃO é apagado no servidor (remover pelo Gerenciador de Arquivos).
deployment:
  tasks:
    - export DEPLOYPATH=/home/talamone/public_html/
    - /bin/cp -R index.html sw.js nebulosa sql dados ia $DEPLOYPATH
